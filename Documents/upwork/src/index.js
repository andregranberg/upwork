import React from 'react';
import ReactDOM from 'react-dom';
import PieChart from './testChart.js'

ReactDOM.render(
  <React.StrictMode>
    <PieChart />
  </React.StrictMode>,
  document.getElementById('root')
);
